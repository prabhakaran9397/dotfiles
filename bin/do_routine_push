#!/bin/bash

if [ $1 ] && [ -d $1 ]; then
	cd $1
	if [ -d .git ]; then
        if [ "$(git status | grep 'git add')" ]; then
		    git add -A
		    git commit -m "routine commit $(date '+%d-%b-%Y-%I:%M%p')"
        fi
		git push origin master
	fi
fi
